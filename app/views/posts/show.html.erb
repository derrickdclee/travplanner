<div class="container box">
  <p id="notice"><%= notice %></p>
  <div class="intro">
    <p>
      <h1><strong>Destination: <%= @post.title %></strong></h1>
    </p>
    <p>
      <h2><i>"<%= @post.content %>"</i></h2>
    </p>
    <%= link_to 'Edit or Invite a New Travel Buddy', edit_post_path(@post) %> |
    <%= link_to 'Back', posts_path %>
  </div>
  
  <%= render 'traveldays/display_travelday' %>
</div>