<%= form_for(travelday) do |f| %>
  <% if travelday.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(travelday.errors.count, "error") %> prohibited this travelday from being saved:</h2>

      <ul>
      <% travelday.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
